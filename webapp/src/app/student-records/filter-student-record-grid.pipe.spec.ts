import { FilterStudentRecordGridPipe } from './filter-student-record-grid.pipe';

describe('FilterStudentRecordGridPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStudentRecordGridPipe();
    expect(pipe).toBeTruthy();
  });
});
